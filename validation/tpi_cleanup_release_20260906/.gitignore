# Rebuilt/archived executables and local Python cache are not source checkpoints.
/previous_release/
/precleanup_rebuilt/
__pycache__/
*.py[cod]

# Keep run commands, hashes and solver logs; omit raw particle output/conversions.
/runs/*
!/runs/*/
/runs/*/*
!/runs/*/Run.out
!/runs/*/run_metadata.json
