# C5g Surface Residual Audit

The detailed particle-field audit compares the coarse and one-level-finer normalized mode-4 sphere diagnostics.

For the raw mode-4 control at the final retained frame (`t=0.006032 s`), the `r>0.85R` surface layer has mean excess `86.5495 Pa`, p95 absolute excess `131.557 Pa`, and max absolute excess `182.958 Pa`.

Classification: `systematic`. The residual is not explained by the ghost residual diagnostic, because that log diagnostic samples the prescribed ghost state rather than the full material surface layer through time.

The radial-bin CSV records whether residual changes are concentrated near `0.95R-1.0R` or spread through the outer shell.
